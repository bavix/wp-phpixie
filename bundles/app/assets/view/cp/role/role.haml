- $this->layout('app:cp/layout')

.tabs-container
  .tabs-left
    %ul.nav.nav-tabs
      - foreach ($roles as $item => $role)
        %li{:class => $item === 0 ? 'active' : ''}
          %a{"aria-expanded" => "true", "data-toggle" => "tab", :href => "#role-" . $role->id()}
            = $role->title
            %label.label.label-danger.pull-right
              = $role->id
    .tab-content
      - foreach ($roles as $item => $role)
        %div.tab-pane{:id => 'role-' . $role->id(), :class => $item === 0 ? 'active' : ''}
          .panel-body
            %table.table.table-bordered.table-striped
              %thead
                %tr
                  %td
                  %td Right of viewing
                  %td Right of addition
                  %td Right of editing
                  %td Right of removal
                  %td Pull Request
              %tbody
                %tr
                  %td Section of Wheels

                  %td
                    - if ($role->hasMyPermission('cp.sow.wheel'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.sow.wheel'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no
                  %td
                    - if ($role->hasMyPermission('cp.sow.wheel.add'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.sow.wheel.add'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no
                  %td
                    - if ($role->hasMyPermission('cp.sow.wheel.edit'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.sow.wheel.edit'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no
                  %td
                    - if ($role->hasMyPermission('cp.sow.wheel.delete'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.sow.wheel.delete'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no
                  %td
                    - if ($role->hasPermission('cp.sow.wheel.delete'))
                      %span.label.label-success master
                    - else if ($role->hasMyPermission('cp.sow.wheel.pull-request'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.sow.wheel.pull-request'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no

                %tr
                  %td Section of Brands

                  %td
                    - if ($role->hasMyPermission('cp.catalogue.brand'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.catalogue.brand'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no
                  %td
                    - if ($role->hasMyPermission('cp.catalogue.brand.add'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.catalogue.brand.add'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no
                  %td
                    - if ($role->hasMyPermission('cp.catalogue.brand.edit'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.catalogue.brand.edit'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no
                  %td
                    - if ($role->hasMyPermission('cp.catalogue.brand.delete'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.catalogue.brand.delete'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no
                  %td
                    - if ($role->hasPermission('cp.catalogue.brand.delete'))
                      %span.label.label-success master
                    - else if ($role->hasMyPermission('cp.catalogue.brand.pull-request'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.catalogue.brand.pull-request'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no

                %tr
                  %td Section of Users

                  %td
                    - if ($role->hasMyPermission('cp.user'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.user'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no
                  %td
                    - if ($role->hasMyPermission('cp.user.add'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.user.add'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no
                  %td
                    - if ($role->hasMyPermission('cp.user.edit'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.user.edit'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no
                  %td
                    - if ($role->hasMyPermission('cp.user.delete'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.user.delete'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no
                  %td
                    - if ($role->hasPermission('cp.user.delete'))
                      %span.label.label-success master
                    - else if ($role->hasMyPermission('cp.user.pull-request'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.user.pull-request'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no

                %tr
                  %td Section of Roles

                  %td
                    - if ($role->hasMyPermission('cp.role'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.role'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no
                  %td
                    - if ($role->hasMyPermission('cp.role.add'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.role.add'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no
                  %td
                    - if ($role->hasMyPermission('cp.role.edit'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.role.edit'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no
                  %td
                    - if ($role->hasMyPermission('cp.role.delete'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.role.delete'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no
                  %td
                    - if ($role->hasPermission('cp.role.delete'))
                      %span.label.label-info master
                    - else
                      %span.label.label-default none

                %tr
                  %td Section of Permissions

                  %td
                    - if ($role->hasMyPermission('cp.permission'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.permission'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no
                  %td
                    - if ($role->hasMyPermission('cp.permission.add'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.permission.add'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no
                  %td
                    - if ($role->hasMyPermission('cp.permission.edit'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.permission.edit'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no
                  %td
                    - if ($role->hasMyPermission('cp.permission.delete'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.permission.delete'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no
                  %td
                    - if ($role->hasPermission('cp.permission.delete'))
                      %span.label.label-info master
                    - else
                      %span.label.label-default none

                %tr
                  %td Section of Dealers

                  %td
                    - if ($role->hasMyPermission('cp.catalogue.dealer'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.catalogue.dealer'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no
                  %td
                    - if ($role->hasMyPermission('cp.catalogue.dealer.add'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.catalogue.dealer.add'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no
                  %td
                    - if ($role->hasMyPermission('cp.catalogue.dealer.edit'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.catalogue.dealer.edit'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no
                  %td
                    - if ($role->hasMyPermission('cp.catalogue.dealer.delete'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.catalogue.dealer.delete'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no
                  %td
                    - if ($role->hasPermission('cp.catalogue.dealer.delete'))
                      %span.label.label-success master
                    - else if ($role->hasMyPermission('cp.catalogue.dealer.pull-request'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.catalogue.dealer.pull-request'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no

                %tr
                  %td Section of Headings

                  %td
                    - if ($role->hasMyPermission('cp.catalogue.heading'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.catalogue.heading'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no
                  %td
                    - if ($role->hasMyPermission('cp.catalogue.heading.add'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.catalogue.heading.add'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no
                  %td
                    - if ($role->hasMyPermission('cp.catalogue.heading.edit'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.catalogue.heading.edit'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no
                  %td
                    - if ($role->hasMyPermission('cp.catalogue.heading.delete'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.catalogue.heading.delete'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no
                  %td
                    - if ($role->hasPermission('cp.catalogue.heading.delete'))
                      %span.label.label-success master
                    - else if ($role->hasMyPermission('cp.catalogue.heading.pull-request'))
                      %span.label.label-primary yes
                    - else if ($role->hasPermission('cp.catalogue.heading.pull-request'))
                      %span.label.label-warning yes
                    - else
                      %span.label.label-danger no